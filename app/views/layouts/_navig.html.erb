<nav class="navbar navbar-expand-lg fixed-top navbar-dark bg-dark d-flex justify-content-between">
      
        <% if current_user %>
          <h1 class="navbar-brand"><%= current_user.shop.name + " (" + current_user.name + ")" %></h1>
        
          <ul class="navbar-nav">
            <li class="nav-item"> <%= link_to 'Home', shop_path(current_user.shop), class:"nav-link" %> </li>
            <li class="nav-item"> <%= link_to 'Shops', shops_path, class:"nav-link" %> </li>
            <li class="nav-item"> 
              <%= link_to shop_requests_path(current_user.shop.id), class:"nav-link" do %>
                Requests <span class="badge badge-light"><%= Request.where(supplier_id: current_user.shop.id , status: "Not Processed").count %></span>    
              <% end %>
            </li>
            <li class="nav-item"> <%= link_to 'Logout', destroy_user_session_path, method: :delete, class:"nav-link" %> </li>
          </ul>
        <% else %>
          <h1 class="navbar-brand">Autmobile Industry</h1>
        
          <ul class="navbar-nav">
            <li class="nav-item"> <%= link_to 'SignUp', new_registration_path(resource_name), class:"nav-link" %> </li>
            <li class="nav-item"> <%= link_to 'Login', new_user_session_path, class:"nav-link" %> </li>
          </ul>
        <% end %>
  
      </nav>